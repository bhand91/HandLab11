@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href=".\Professor">View Professors and Courses</a>.</p>
</div>

<div class = "container">
    <div class = "row">
        <div class = "col-9">
            @foreach(var p in Model.Professors)
            {
                <li>@p.FirstName
                    @p.LastName
                </li>
            }
        </div>
    </div>
</div>



